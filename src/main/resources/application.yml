server:
  port: 8080

spring:
  application:
    name: token-service
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0

jwt:
  secret: your-256-bit-secret-key-must-be-at-least-32-characters-long
  expiration: 3600000
